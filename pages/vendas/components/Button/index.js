export const Button = (props) => (
  <div className="rounded-full p-2 border-2 border-green w-full">
    <button
      className="p-6 rounded-full bg-green font-bold w-full"
    >
      Garantir minha vaga
      </button>
  </div>
);
